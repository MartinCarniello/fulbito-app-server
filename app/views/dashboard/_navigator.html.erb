<nav role="navigation" class="navbar navbar-inverse">

  <div class="navbar-header">
    <span class="navbar-brand">FutbolApp</span>
  </div>

  <div id="navbarCollapse" class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <li ng-class="{active: navOption === 0}"><a href="#" ng-click="home($event);">Home</a></li>
      <li ng-class="{active: navOption === 1}"><a href="#" ng-click="friends($event);">Friends</a></li>
      <li ng-class="{active: navOption === 2}"><a href="#" ng-click="matchs($event);">Matchs</a></li>
      <li ng-class="{active: navOption === 3}"><a href="#" ng-click="tournament($event);">Tournament</a></li>
    </ul>
  </div>
</nav>